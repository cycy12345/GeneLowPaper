% Generated by generate_rd.py
\name{BM_DEG_analysis}
\alias{BM_DEG_analysis}
\title{Integrated Differential Expression Analysis
}
\description{
Performs differential expression analysis using multiple methods.

When \code{is_count = TRUE}, runs DESeq2, limma-voom, and edgeR on raw counts.

When \code{is_count = FALSE}, runs limma and Wilcoxon rank-sum test on normalized data.


If \code{FALSE}, the input is treated as normalized expression matrix.

If \code{NULL}, groups are inferred from TCGA barcodes (sample type 01-09 = tumor, 10-19 = normal).


\code{deg_edger}. For normalized data: \code{deg_limma}, \code{deg_wilcoxon}.
}
\usage{
BM_DEG_analysis(exprset, project = NULL, is_count = TRUE, logFC_cut = 0, deg_filter = FALSE, pvalue_cut = 1, adjpvalue_cut = 1, group = NULL, save = FALSE)
}
\arguments{
  \item{exprset}{A gene expression matrix with genes as rows and samples as columns.}
  \item{project}{Character string used as the file name prefix when \code{save = TRUE}.}
  \item{is_count}{Logical. If \code{TRUE}, the input is treated as raw count matrix.}
  \item{logFC_cut}{Numeric. Log2 fold change threshold for filtering DEGs. Default is 0.}
  \item{pvalue_cut}{Numeric. P-value threshold for filtering DEGs. Default is 1.}
  \item{adjpvalue_cut}{Numeric. Adjusted p-value threshold for filtering DEGs. Default is 1.}
  \item{deg_filter}{Logical. Whether to filter results by the thresholds above. Default is \code{FALSE}.}
  \item{group}{A factor vector of group labels for each sample. Control should be the first level.}
  \item{save}{Logical. Whether to save results to disk. Default is \code{FALSE}.}
}
\details{
The function automatically detects whether log2 transformation is needed for

non-count data by inspecting quantiles.


}
\value{
A named list of DEG results. For count data: \code{deg_deseq2}, \code{deg_limma},
}
\examples{
\dontrun{

# Example with raw counts

res <- BM_DEG_analysis(exprset = count_matrix, project = "LUAD",

is_count = TRUE, group = group_factor, save = TRUE)


# Example with normalized data

res <- BM_DEG_analysis(exprset = norm_matrix, is_count = FALSE,

logFC_cut = 1, pvalue_cut = 0.05, deg_filter = TRUE)

}


}
\author{GeneLowPaper package}
